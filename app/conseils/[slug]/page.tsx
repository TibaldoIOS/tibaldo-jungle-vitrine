import Link from "next/link";
import type {Metadata} from "next";import {notFound} from "next/navigation";import ScrollReveal from "../../ScrollReveal";import {SiteFooter,SiteHeader} from "../../SiteChrome";import {getGuide,guides} from "@/lib/guides/catalog";
export function generateStaticParams(){return guides.map(({slug})=>({slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const guide=getGuide((await params).slug);if(!guide)return{};return{title:`${guide.title} | Tibaldo Jungle Lille`,description:guide.intro,alternates:{canonical:`/conseils/${guide.slug}`}}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const guide=getGuide((await params).slug);if(!guide)notFound();const isPotGuide=guide.slug==="pot-perce-cache-pot-coupelle";const schema={"@context":"https://schema.org","@type":"Article",headline:guide.title,description:guide.intro,author:{"@type":"Organization",name:"Studio Végétal — Tibaldo Jungle"},mainEntityOfPage:`https://jungle.tibaldo.fr/conseils/${guide.slug}`};return <main className={`editorial-page guide-detail${isPotGuide?" is-pot-guide":""}`}><ScrollReveal/><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><section className="inner-hero compact-inner-hero"><div className="inner-hero-texture"/><div className="inner-hero-shade"/><SiteHeader/><div className="shell inner-hero-content"><p className="eyebrow"><span/> {guide.eyebrow} · Guide pratique</p><h1><span className="hero-line"><span>{guide.title}</span></span></h1><p>{guide.intro}</p></div></section>{isPotGuide&&<section className="shell pot-choice-summary" data-reveal><header><p className="section-kicker">La règle en un regard</p><h2>Le trou reste<br/><em>non négociable.</em></h2></header><div><article><span>01 · Recommandé</span><h3>Pot percé<br/>dans un cache-pot</h3><p>Décoratif et facile à contrôler. Sortez, arrosez, laissez égoutter puis videz le cache-pot.</p></article><article><span>02 · Recommandé</span><h3>Pot percé<br/>avec coupelle</h3><p>L’excédent devient visible. La coupelle doit être vidée après chaque arrosage.</p></article><article className="is-warning"><span>03 · À éviter</span><h3>Plantation directe<br/>dans un pot fermé</h3><p>L’eau ne peut pas sortir et les racines finissent par manquer d’air.</p></article></div></section>}<article className="shell guide-article"><div className="guide-toc"><span>Dans ce guide</span>{guide.sections.map(([title],index)=><a key={title} href={`#${title.toLowerCase().replaceAll(" ","-")}`}><small>{String(index+1).padStart(2,"0")}</small>{title}</a>)}</div><div>{guide.sections.map(([title,copy],index)=><section className={isPotGuide?(index%2===0?"reveal-left":"reveal-right"):undefined} id={title.toLowerCase().replaceAll(" ","-")} key={title} data-reveal><span>0{index+1}</span><h2>{title}</h2><p>{copy}</p></section>)}<aside><h2>Un doute sur le contenant ?</h2><p>Apportez votre plante et son pot au Studio Végétal : nous vérifions ensemble le drainage, le volume et le substrat.</p><Link className="button button-green" href="/rempotage">Bar à rempotage ↗</Link></aside></div></article><SiteFooter/></main>}
