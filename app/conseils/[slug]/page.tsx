import type {Metadata} from "next";import {notFound} from "next/navigation";import ScrollReveal from "../../ScrollReveal";import {SiteFooter,SiteHeader} from "../../SiteChrome";import {getGuide,guides} from "@/lib/guides/catalog";
export function generateStaticParams(){return guides.map(({slug})=>({slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const guide=getGuide((await params).slug);if(!guide)return{};return{title:`${guide.title} | Tibaldo Jungle Lille`,description:guide.intro,alternates:{canonical:`/conseils/${guide.slug}`}}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const guide=getGuide((await params).slug);if(!guide)notFound();const schema={"@context":"https://schema.org","@type":"Article",headline:guide.title,description:guide.intro,author:{"@type":"Organization",name:"Studio Végétal — Tibaldo Jungle"},mainEntityOfPage:`https://jungle.tibaldo.fr/conseils/${guide.slug}`};return <main className="editorial-page guide-detail"><ScrollReveal/><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><section className="inner-hero compact-inner-hero"><div className="inner-hero-texture"/><div className="inner-hero-shade"/><SiteHeader/><div className="shell inner-hero-content"><p className="eyebrow"><span/> {guide.eyebrow} · Guide pratique</p><h1><span className="hero-line"><span>{guide.title}</span></span></h1><p>{guide.intro}</p></div></section><article className="shell guide-article"><div className="guide-toc"><span>Dans ce guide</span>{guide.sections.map(([title])=><a key={title} href={`#${title.toLowerCase().replaceAll(" ","-")}`}>{title}</a>)}</div><div>{guide.sections.map(([title,copy],index)=><section id={title.toLowerCase().replaceAll(" ","-")} key={title} data-reveal><span>0{index+1}</span><h2>{title}</h2><p>{copy}</p></section>)}<aside><h2>Besoin d’un regard extérieur ?</h2><p>Apportez votre plante au Studio Végétal à Lille ou commencez par notre diagnostic guidé.</p><a className="button button-green" href="/sos-plantes">SOS Plantes ↗</a></aside></div></article><SiteFooter/></main>}
