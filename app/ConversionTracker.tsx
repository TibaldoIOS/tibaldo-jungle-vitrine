"use client";import{useEffect}from"react";
export default function ConversionTracker(){useEffect(()=>{const handler=(event:MouseEvent)=>{const link=(event.target as Element|null)?.closest("a");if(!link)return;let action=link.getAttribute("data-action");const href=link.getAttribute("href")??"";if(!action&&href.startsWith("mailto:"))action="email";if(!action&&href.includes("facebook.com"))action="facebook";if(!action)return;const body=JSON.stringify({action,path:location.pathname,target:href});if(navigator.sendBeacon)navigator.sendBeacon("/api/conversions",new Blob([body],{type:"application/json"}));else void fetch("/api/conversions",{method:"POST",headers:{"Content-Type":"application/json"},body,keepalive:true})};document.addEventListener("click",handler);return()=>document.removeEventListener("click",handler)},[]);return null}
